// Gets item names from lunr results, get data from my JSON and call onAdd={addTask}
const Item = ({item}) => {
    return (
        <div>
            {item}
        </div>
    )
}

export default Item