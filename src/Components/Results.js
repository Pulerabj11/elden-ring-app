const Results = ({results}) => {
    // var data = results[0]
    // var data2 = JSON.parse(data)
    // var data3 = data2.ref
    return (
        <div>
            {JSON.stringify(results)}
        </div>
    )
}

export default Results